---
layout: post
title: cs231n Lecture4
subtitle: 신경망 기초(Introduction to Neural Networks)
show-avatar: true
category: study
tags: [cs231n, Deep Learning, Computer Vision]
comments: true
---

Where we are
- scores function, SVM loss, data loss + regularization
- Optimization (Gradient Descent)
- Gradient descent(Numerical gradient, Analytic gradient)



How do we find the best W?
-> Optimization: walking around large valley(height: loss)
Strategy #1: A first very bad idea solution: Random search
Strategy #2: Follow the slope(feel the local geometry)
In multiple dimensions, the gradient is the vector of (partial derivatives)
along each dimension
The slope in any direction is the dot product fo the direction with the gradient
The direction of steepest descent is the negative gradient

Numerical gradient: approximate, slow, easy to write
current W -> W + h -> gradient dW

This is silly. The loss is just a function of W
Calculus! Use calculus to compute an analytic gradient

Analytic gradient: exact, fast, error-prone
current W -> gradient dW

In practice: Always use analytic gradient, but check implementation with numerical gradient.
This is called a gradient check. (using as debugging tool)

Gradient Descent

Stochastic Gradient Descent(SGD)
Full sum expensive when N is large!
Approximate sum using a minibatch of examples (32/ 64/ 128 common)

Aside: Image Features

Image Features: Motivation
Cannot separate red and blue points with linear classifier.
After applying feature trasform,
points can be seperated by linear classifier.

Example: Color Histogram
color buckets

Example: Histogram of Oriented Gradients(HoG)
compute a histogram over these differnet edge orientations

Example: Bag of Words
1) Build codebook
Extract random patches -> Cluster patches to form "codebook" of "visual words"
2) Encode images

Image features vs. ConvNets
Image -> Feature Extraction -> 10 numbers giving scores for classes
Image -> ConvNets -> 10 numbers giving scores for classes
    <-------------------

Next time:
Introduction to neural networks
Backpropagation



Computational graphs
x, W -> f=Wx -> scores -> hinge loss -> +R(W) -> L
Back propagation

Convolutional network(AlexNet)
input image -> weights -> loss

Neural Turing Machine

Backpropagation
f(x,y,z) = (x+y)z
x= -2, y= 5, z= -4
q = x + y, dq/dx=1, dq/dy=1
f = qz, df/dq=z, df/dz=q
want: df/dx, df/dy, df/dz
df/df=1
df/dz=q=3
df/dq=z=-4
df/dx=df/dq*dq/dx=-4*1=-4
df/dy=df/dq*dq/dy=-4*1=-4
chain rule

x(dL/dx), y(dL/dy) -> f(dz/dx, dz/dy) -> z
                                      <- dL/dz
dz/dx, dz/dy: "local gradient" 

sigmoid function : 1/(1+e^-x)
sigmoid gate : *-1 -> exp -> +1 -> 1/x

patterns in backward flow
add gate: gradient distributor(동등하게 나눠줌)
max gate: gradient router(한쪽에 몰빵함)
mul gate: gradient switcher(합이 0이 됨)

Gradients add at branches

Gradients for vectorized code
x, y, z are now vectors

dz/dx : This is now the Jacobian matrix(야코비 행렬)
(derivative of each element of z with respect to each element of x)

Vectorized operations
4096-d input vector -> f(x)=max(0,x) (elementwise) -> 4096-d output vector
dL/dx = dL/df*df/dx
Q. What's the size of the Jacobian matrix(df/dx)?
A. [4096 x 4096]

in practive we process an entire minibatch(e.g. 100) of examples at one time
i.e Jacobian would technically be a [409600 x 409600] matrix
-> diagonal matrix(대각행렬)

A vectorized example
Always check: The gradient with respect to a variable should have the
same shape as the variable

class ComputationalGraph(object):
	def forward(inputs):
		#1. pass inputs to input gates
		#2. forward the computational graph
		for gate in self.graph.nodes_topologically_sorted():
			gate.forward()
		return loss # the final gate in the graph outputs the loss
	def backward():
		for gate in reversed(self.graph.nodes_topologically_sorted()):
			gate.backward() # little piece of backprop (chain rule applied)
		return input_gradients

class MultiplyGate(object):
	def forward(x, y):
		z = x*y
		self.x = x # must keep these around!
		self.y = y
		return z
	def backward(dz):
		dx = self.y * dz # [dL/dz*dz/dx]
		dy = self.x * dz # [dL/dz*dz/dy]
		return [dx, dy]

In Assignment 1: Writing SVM / Softmax
Stage your forward/backward computation!

backpropagation =
recursive application of the chain rule along a computaitional graph
to compute the gradients of all inputs/parameters/intermediates

forward : 
compute result of an operation and save any intermediates needed for
gradient computation in memory

backward : 
apply the chain rule to compute the gradient of the loss function with
respect to the inputs

Neural networks: without the brain stuff
(Before) Linear score function: f = Wx
(Now) 2-layer Neural Network: f= W_2max(0,W_1x)
3-layer Neural Network: f= W_3max(0,W_2max(0,W_1x))
x -> W1 -> h -> W2 -> s
h=max(0,W_1x)

In Assignment 2: Writing a 2-layer net

Neurons
axon from a neuron - synapse - dendrite - cell body - output axon

Activation functions
Sigmoid, tanh, ReLU, Leaky ReLU, Maxout, ELU

Neural Networks: Architectures
"2-layer Neural Net" or "1-hidden-layer Neural Net"

Example feed-forward computation of a neural network

class Neuron:
	def neuron_tick(inputs):
		cell_body_sum = np.sum(inputs * self.weights) + self.bias
		firing_rate = 1.0 / (1.0 + math.exp(-cell_body_sum)) # sigmoid activation function
		return firing_rate

f = lambda x: 1.0/(1.0 + np.exp(-x))
x = np.random.randn(3, 1)
h1 = f(np.dot(W1, x) + b1)
h2 = f(np.dot(W2, h1) + b2)
out = np.dot(W3, h2) + b3

Summary
The abstraction of a layer has the nice property that it allows us to
use efficient vectorized code (e.g. matrix multiplies)
