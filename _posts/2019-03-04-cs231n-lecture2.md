---
layout: post
title: cs231n Lecture2
subtitle: 이미지 분류(Image Classification)
show-avatar: true
category: study
tags: [cs231n, Deep Learning, Computer Vision]
comments: true
---

## 1. Challenges
  - Viewpoint variation
  - Illumination
  - Deformation
  - Occlusion
  - Background Clutter
  - Intraclass variation
### The Problem is "Semantic Gap"

## 2. An Image Classifier
- Deep Learning의 도래로 인해 "Data-Driven Approach"로 변화
- CIFAR10

## 3. Nearest Neighbor
Distance Metric to compare images (L1 distance)

## 4. K-Nearest Neighbors
Instead of copying label from nearest neighbor, take majority vote form K closest points

## 5. Distance metric
- L1 (Manhattan) distance
- L2 (Euclidean) distance

## 6. Hyperparameters
- What is the best value of k to use?
- What is the best distance to use?

These are hyperparameters choices about the algorithm that we set rather than learn

## 7. Setting Hyperparameters
- Idea #1 : Choose hyperparameters that work best on the data

  BAD: K=1 always works perfectly on training data
- Idea #2 : Split data into train and test, choose hyperparameters that work best on test data

  BAD: No idea how algorithm will perform on new data
- Idea #3 : Split data into train, val, and test; choose hyperparameters on val and evaluate on test

  Better!
- Idea #4 : Cross-Validataion: Split data into folds, try each fold as validation and average the results

  Useful for small datasets, but not used too frequently in deep learning

## 8. k-Nearest Neighbor on images never used.
- Very slow at test time
- Distance metrics on pixels are not informative
- Curse of dimensionality

## 9. Linear Classification

> Karpathy and Fei-Fei "Deep Visual-Semantic Alignments for Generating Image Descriptions"

- Parametric Approach  
  Image -> f(x,W) -> 10 number giving class scores  
  f(x,W) = Wx + b  
  [10x1] [10x3072] [3072x1] [10x1]

- Linear Classifier -> 1 template for a class

## 10. How can we tell whether this W is good or bad?
- Loss Function : quantifiying what it means to have a "good" W
- ptimization : start with random W and find a W that minimizes the loss
- ConvNets : tweak the functional form of f
