---
layout: post
title: cs231n Lecture6
subtitle: 신경망 학습 1(Training Neural Networks 1)
show-avatar: true
category: study
tags: [cs231n, Deep Learning, Computer Vision]
comments: true
---

fine tuning
1. Train on ImageNet
2. Finetune network on your own data

Transfer Learning with CNNs
1. Train on ImageNet
2. If small dataset: fix all weights:
(treat CNN as fixed feature extractor), retrain only the classifier
i.e. swap the Softmax layer at the end (FC-1000, softmax)
3. If you have medium sized dataset, "finetune"
instead: use the old weights as initialization,
train the full network or only some of the higher layers
retrain bigger portion of the network, or even all of it.

Lots of pretrained ConvNets

--------------------------------------------------------------------

Where we are now

Computational Graph
Neural Networks
Convolutional Neural Networks
Convolutional Layer
Image - filter - activation map(s) - new Image

Learning network parameters through optimization
Mini-batch SGD
Loop:
1. 'Sample' a batch of data
2. 'Forward' prop it through the graph(network), get loss
3. 'Backprop' to calculate the gradients
4. 'Update' the parameters using the gradient

Next: Training Neural Networks
Overview
1. One time setup
activation functions, preprocessing, weight initialization,
regularization, gradient checking
2. Training dynamics
babysitting the learning process,
parameter updates, hyperparameter optimization
3. Evaluation
model ensembles

Part1
- Activation Functions
- Data Preprocessing
- Weight Initialization
- Batch Normalization
- Babysitting the Learning Process
- Hyperparameter Optimization

Activation Functions
- Sigmoid, tanh, ReLU, Leaky ReLU, PReLU, ELU

Sigmoid
- Squashes numbers to range [0,1]
- Historically popular since they have nice interpretation
as a saturating "firing rate" of a neuron

3 problems:
1) Saturated neurons "kill" the gradients (vanishing gradient)
x = -10 flat , x = 0 fine, x= 10 flat
(saturated regime)

2) Sigmoid outputs are not zero-centered (slow convergence)
f = w_ix_i+b
df/dw_i = x_i
dL/dw_i = dL/df * df/dw_i = dL/df * x_i(always positive)
-> gradients always positive 

Consider what happens when the input to a neuron (x) is always positive
What can we say about the gradients on w?
always all positive or all negative
(this is also why you want zero-mean data!)

3) exp() is a bit compute expensive

tanh(x) : hyperbolic tangent / LeCun et al., 1991
- Squashes numbers to range [-1, 1]
- zero centered (nice)
- still kills gradients when saturated

ReLU(Rectified Linear Unit)
- Computes f(x) = max(0, x)
- Does not saturate (in +region)
- Very computationally efficient
- Converges much faster than sigmoid/tanh in practice(e.g. 6x)
- Actually more biologically plausible than sigmoid
- Not zero-centered output
- An annoyance: (hint: what is the gradient when x < 0?)
x = -10: 0, x = 0: 0, x = 10: 1
data cloud(my training data)
active ReLU
dead ReLU will never activate(never update) :
initialize learning rate 
people like to initialize ReLU neurons with slightly positive biases
(e.g. 0.01)

Leaky ReLU / Mass et al., 2013, He et al., 2015
f(x) = max(0.01x, x)
- Does not saturate
- Computationally efficient
- Converges much faster than sigmoid/tanh in practice! (e.g. 6x)
- will not "die"

Parametric Rectifier(PReLU)
f(x) = max(ax, x)
train alpla(a)
backprop into a(parameter)

Exponential Linear Units(ELU) / Clevert et al., 2015
- All benefits of ReLU
- Closer to zero mean outputs
- Negative saturation regime compared with Leaky ReLU adds
some robustness to noise
- Computation requires exp()

Maxout "Neuron" / Goodfellow et al., 2013
- Does not have the basic form of dot product -> nonlinearity
- Generalizes ReLU and Leaky ReLU
- Linear Regime! Does not saturate! Does not die!
max(w^T_1x+b_1, w^T_2x+b_2)
problem: doubles the number of parameters/neuron

TLDR: In practice:
- Use ReLU. Be careful with your learning rates
- Try out Leaky ReLU/ Maxout / ELU
- Try out tanh but don't expect much
- Don't use sigmoid

Data Preprocessing
Step 1: Preprocess the data
original data -> zero-centered data (-> normalized data)
X-=np.mean(X, axis=0) , X/=np.std(X, axis=0)
(Assume X[NxD] is data matrix, each example in a row)

Remember: Consider what happens when the input to a neuron is always positive...
weights all positive or all negative!

In practice, you may also see PCA and Whitening of the data
original data -> decorrelated data -> whitened data
decorrelated data: data has diagonal covariance matrix
whitened data: covariance matrix is the identity matrix

TLDR: In practice for Images: center only
e.g. consider CIFAR-10 example with [32,32,3] images
- Subtract the mean image(e.g. AlexNet)
(mean image of entire training image = [32,32,3] array)
- Subtract per-channel mean(e.g. VGGNet)
(mean along each channel RGB = 3 numbers)
Not common to normalize variance, to do PCA or whitening
(PCA: Principal Component Analysis 주성분분석 데이터의 차원을 줄임)
(Whitening: 이미지라면 인접 픽셀간의 인접성(redundancy)을 줄여줌)

Weight Initialization
Q. what happens when W=0 init is used?
all the neurons will do the same thing(same operation)
backpropagation -> same gradient
same output, same gradient, same update
No symmetry breaking

- First Idea: Use small random numbers 
(gaussian with zero mean and 1e-2 standard deviation)
W = 0.01 * np.random.randn(D, H)

Works okay for small networks, but problems with deeper networks.
can lead to non-homogeneous distributions of activations across
the layers of a network.

Lets look at some activation statistics
E.g. 10-layer net with 500 neurons on each layer,
using tanh non-linearities, and initializing as described in last slide.

All activations become zero!

Q. think about the backward pass. What do the gradients look like?
Hint: think about backward pass for a W*X gate.
dW_1 = X * dW_2 -> vanishing gradient

*1.0 instead of 0.01
W is too big -> overshooting
-> Almost all neurons completely saturated, either -1 and 1.
-> Gradients will be zero.

"Xavier initialization" / Glorot et al., 2010
input large-weight small, input small-weight large
Reasonable initialization
(Mathematical derivation assumes linear activations)
fits well on tanh
but when using the ReLU nonlinearity it breaks.

He et al., 2015 (note additional/2)
fits well on ReLU 

Proper initialization in an active area of research

Batch Normalization / loffe and Szegedy, 2015
"you want unit gaussian activations? just make them so"

consider a batch of activations at some layer.
To make each dimension unit gaussian, apply: ~
this is a vanilla differentiable function

1. compute the empirical mean and variance independently for each dimension
2. Normalize

Usually inserted after Fully Connected (or Convolutional layers), and before nonlinearity.

Problem: do we necessarily want a unit gaussian input to a tanh layer?

Normalize ->
And then allow the network to squash the range if it wants to ->
Note, the network can learn r(gamma), B(betha) to recover the identity mapping

mini-batch mean ->
mini-batch variance ->
normalize ->
scale and shift

- improves gradient flow through the network
- Allows higher learning rates
- Reduces the strong dependency on initialization
- Acts as a form of regularization in a funny way, and slightly reduces the need for dropout, maybe

Note: At test time BatchNorm layer functions differently:
The mean/std are not computed based on the batch.
Instead, a single fixed empirical mean of activations during training is used.
(e.g. can be estimated during training with running averages)

Babysitting the Learning Process
1. Preprocess the Data (Image -> zero-centered)
2. Choose the Neural Network Architecture
3. Double check that the loss is reasonable
0 -> 1e3 (crank up regularization)
4. Make overfitting by using small portion of the training data
- take the first 20 examples from CIFAR-10
- turn off regularization (reg = 0.0)
- use simple vanilla SGD
잘 동작하는지 확인하는 과정
5. Learning rate : Start with small regularization and find learning rate that makes the loss go down.

Loss barely changing: Learning rate is probably too low

Notice train/val accuracy goes to 20% though,
what's up with taht? (remember this is softmax)
loss가 감소하지 않는데도, score에 대해서는 학습이 진행되었기 때문.

cost: NaN almost always means high learning rate

loss not going down : learning rate too low
loss exploding: learning rate too high
Rough range for learning rate we should be cross-validating is somewhere 1e-3 ~ 1e-5

Hyperparameter Optimization

Cross-validation strategy
coarse -> fine cross-validation in stages
First stage: only a few epochs to get rough idea of what params work
Second stage: longer running time, finer search
... (repeat as necessary)

Tip for detecting explosions in the solver:
If the cost is ever > 3 * original cost, break out early

For example:
run coarse search for 5 epochs, note it's best to optimize in log space
Now run finer search..., adjust range 
-> random search
-> grid search

Random Search vs. Grid Search / Random Search for Hyper-Parameter Optimization, Bergstra and Bengio, 2012

Hyperparameters to play with:
- network architecture (#hidden layer, #node, model)
- learning rate, its decay schedule, update type
- regularization(L2/Dropout strength)
turn table = neural networks / music = loss function

Cross-validation "command center"

Monitor and visualize the loss curve:

Bad initialization a prime suspect

lossfunctions.tumblr.com, Loss function specimen
reinforcement learning -> zigzag

Monitor and visualize the accuracy:
loss function: 0.278 vs. accuracy: 0.92
-> can know the meaning of accuracy

training accuracy - validation accuracy gap
big gap = overfitting -> increase regularization strength
no gap -> increase model capacity

Track the ratio of weight updates/weight magnitudes:
ratio between te updates and values: ~0.0002 / 0.02 = 0.01(about okay)
want this to be somewhere around 0.001(1e-3) or so

Summary
- Activation Functions (use RELU)
- Data Preprocessing (images: subtract mean)
- Weight Initialization (use Xavier init/ He)
- Batch Normalization (use)
- Babysitting the Learning process
- Hyperparameter Optimization
(random sample hyperparams, in log space when appropriate)
