---
layout: post
title: (ECCV 2018) Graph R-CNN for Scene Graph Generation
subtitle: 
excerpt: ""
show-avatar: true
category: papers
tags: [ECCV, ECCV2018, Scene Graph Generation, Graph R-CNN, Relation Proposal Network, attentional Graph Convoluion Network, SGGen+]
comments: true
---

---

[***Scene Graph***](https://sangminwoo.github.io/2019-11-08-papers-08#Scene-Graph) 

[***Scene Graph Generation***](https://sangminwoo.github.io/2019-11-08-papers-08#Scene-Graph-Generation) 

[***Graph R-CNN***](https://sangminwoo.github.io/2019-11-08-papers-08#Graph-R-CNN) 

[***SGGen+***](https://sangminwoo.github.io/2019-11-08-papers-08#SGGen+) 

[***아이디어***](https://sangminwoo.github.io/2019-11-08-papers-08#아이디어) 

[***정리***](https://sangminwoo.github.io/2019-11-08-papers-08#정리)

[***Reference***](https://sangminwoo.github.io/2019-11-08-papers-08#Reference)

---

# Scene Graph
컴퓨터비전에서 Visual scene understanding의 중요성은 지속적으로 제기되어왔고, 지금까지는 특히 아래의 세 가지 연구(object-centric techniques)에 초점을 맞추어 진행되어 왔습니다.

1) presence(image classification)
2) spatial extent(object detection)
3) pixel-wise spatial extent(semantic segmentation)

하지만 이것만으로는 scene understanding이 충분하다고 보기에는 무리가 있습니다. 따라서 최근에는 단순히 object의 존재 유무, 위치를 아는 것(perception)을 넘어 object의 속성, object와 object간의 관계 등을 추론하고 생각하는 것(cognition)에 대한 연구[1](https://sangminwoo.github.io/2019-11-08-papers-08#[1]])가 대두되었습니다. 

Graph는 이러한 관계를 표현하기 위해서 직관적으로 이해하기 쉬운 구조입니다. 각각의 node를 object 또는 attribute로 보고, edge들을 node끼리의 관계를 규정한다고 생각할 수 있습니다. 이러한 구조를 두고 **Scene Graph**라고 하는데, 근래에 다음과 같이 보다 높은 추론이 요구되는 연구들에 적용되고 있습니다.

-  Image Captioning
-  Visual Question Answering
-  Image-grounded Dialog

문제는 이러한 Scene Graph를 손수 만들기에는 시간과 비용이 많이 드는 일이고, 따라서 "Scene Graph를 어떻게 효율적으로 추출할까?" 하는 의문을 가지게 됩니다. 방법은 여러가지가 있겠지만 우선 가장 쉽게 떠오르는 아이디어부터 정리해봅시다.

1) 모든 노드간의 관계를 엣지로 모조리 연결한다. (Brute Force)

단순하지만 확실한 방법이 될 수는 있습니다. 노드의 숫자가 적을 때는 문제가 되지 않지만 노드의 숫자가 늘어날수록 기하급수적으로 엣지의 숫자가 늘어나게 됩니다. 아무래도 모든 노드를 다 연결하기에는 무리가 있어 보입니다. 그러면 몇개의 노드들에 대해서만 무작위로 관계를 짝지어주는거는 어떨까요?

2) 랜덤하게 샘플링한 엣지들만 고려하는 방법

기존의 scene graph generation 모델들은 이러한 휴리스틱에 의존하는 경향이 컸습니다. 하지만 이는 직관적으로도 굉장히 큰 문제를 안고 있음을 알 수 있습니다. 예를 들어, 이미지 내에 object가 'wheel', 'car', 'building'이 있다고 했을 때 'wheel'-'building'의 관계 보다 'wheel'-'car'의 관계가 당연히 더 일리가 있어 보입니다. 앞선 방법보다는 효율적일 수는 있지만, 역시나 노드간의 관계가 랜덤이 아니기 때문에 논리적으로 타당하다고 보기에는 어려울 듯 합니다.

# Scene Graph Generation

Scene Graph Generation이 크게 두 분류로 나뉘게 되는데
1) Iterative message passing

2) Neural motifs

설명~

![1](/img/papers/08/1.png)
*[2] Scene graph generation by iterative message passing*

```
scene graph is a visually-grounded graph over the object instances in an image, where the edges depict their pairwise relationships.
```

object는 이미지 내에서 bounding box와 label로 표시되고, relationship은 두 개의 bounding box(object-subject) 사이의 directed edge와 predicate(위의 그림에서 빨간색 노드)로 표시됩니다. 

먼저 입력 이미지를 RPN(Region Proposal Network)[3]에 넣으면 object region을 얻을 수 있고, proposed region에 있는 object feature들을 Graph Inference 모델에 넣으면 scene graph가 최종적으로 얻어지는 방식입니다.

기존의 scene graph generation 모델들 또한 앞 단계에서 전달 받은 후보군에 대한 context propagation과 reasoning 과정을 포함하고 있습니다.

[2]에서는 두 개의 sub-graph로 나누어 하나는 object, 하나는 relationship을 추출하고 message passing을 하는 방식으로 동작합니다.



# Graph R-CNN
노드를 추출하는 일은 기존의 object detection의 파이프라인을 따라가면 되지만, 노드 간의 관계를 추정하는 일은 생각보다 쉽지 않습니다. 어떻게 하면 이 관계를 효율적으로 추정할 수 있을까요? 우선 개괄적인 파이프라인을 확인해봅시다.

논문에서는 **Graph R-CNN** 프레임워크를 제시합니다. 이는 다음과 같이 세 단계로 나눌 수 있습니다.

![2](/img/papers/08/2.png)

1) Object node extraction: object detection을 통해 먼저 scene 내의 object(노드)에 대한 bounding box를 그리고, 일단 가능한 모든 relationship(엣지)들을 고려합니다.
여기까지는 기존의 object detection과 크게 다르지 않습니다. 다음의 두 단계에서 등장하는 *RePN*과 *aGCN*이 논문에서 가장 중요한 핵심입니다.

2) Relation edge pruning: **Relation Proposal Network(RePN)**은 연결 가능성이 낮은 relationship(엣지)들을 잘라냅니다. 여기서 *relatedness score*이 등장하는데, 이 score이 낮으면 pruning을 하는 방식입니다. 이렇게 하면 모든 노드가 모두 연결된 처음 graph들에 비해 듬성듬성하게 연결되어 있는 scene graph 후보군을 만들 수 있습니다.

3) Graph context integration: 앞 단계에서 전달받은 scene graph 후보들에 대해 **attentional Graph Convolution Network(aGCN)**를 적용하여 graph 전체에 higher-order context를 전달합니다. 또한 각각의 object(노드)와 relationship(엣지)는 인접한 노드(neighbor)들에 의해 업데이트 됩니다. 이것은 다른 의미로 global context를 통합하는 과정으로 볼 수 있습니다. 기존의 방식과는 다르게, 이 논문에서는 각 노드 별로 엣지 attetion(per-node edge attention)을 예측하는 방식을 사용합니다. 그러면 연결 가능성이 낮은 엣지들에 흐르는 정보를 통제할 수 있도록 학습할 수 있게 됩니다. 위의 그림에서 엣지의 두께가 attention의 정도를 나타냅니다.

# SGGen+
scene graph generation에 대한 기존의 metric은 <subject, predicate, object>에 대한 recall을 계산하거나(SGGen) 또는 object의 위치에 대한 ground truth가 주어졌을 때 object와 predicate에 대한 recall을 계산하는 방식(PredCls, PhrCls)입니다.

하지만 앞선 metric에는 치명적인 단점이 있습니다. 예를 들어, 위의 그림에서 왼쪽 아래의 object를 boy가 아닌 man으로 예측을 했다면, 나머지의 예측이 모두 맞았더라도 전부 오답으로 처리할 수 밖에 없습니다. 가령, 1) standing behind a fire hydrant, 2) near a car, 3) wearing a sweater과 같은 정보들을 정확하게 예측한다고 하더라도 부분점수를 받을 수 없다는 것입니다. 따라서 이러한 triplet-based metric은 작은 실수에도 큰 페널티를 부과한다고 할 수 있습니다.

ground truth가 주어지는 방식은 relationship 예측만을 하기 때문에 이러한 문제를 피해가긴 하지만 역설적이게도 이 때문에 scene graph generation의 성능을 정확히 반영한다고 보기에는 어렵습니다.

따라서 이러한 문제를 해결하기 위해 새로운 metric을 제시하게 됩니다. 여기서 두 개의 duplet과 하나의 triplet을 사용하기 때문에 SGGen+으로 명명한것으로 보입니다.

이는 <object, predicate>, <object, attribute>, <subject, predicate, object>에 대한 recall을 총체적으로 계산합니다.

기존의 triplet 하나의 recall만 계산하는 것에 비해 이렇게 했을 경우에 앞선 문제들을 조금 완화할 수 있습니다.

# 아이디어
In this paper, they propose novel scene graph generation model called Graph R-CNN, that is both effective and efficient at detecting objects and their relations in images. Model includes a Relation Proposal Network(RePN) that efficiently deals with the quadratic number of potential relations between objects in an image by intelligently prunes out pairs of objects that are unlikely to be related. They also propose an attentional Graph Convolution Network(aGCN) that effectively captures contextual information between objects and relations, then propagates across the graph. Finally they introduce a new scene graph generation evaluation metric(SGGen+) that is more fine-grained and realistic than existing metrics. Model reported SOTA performance on scene graph generation as evaluated using both existing and their proposed metrics.

Proposed framework can be factorized into three logical stages as follows:
1) Object node extraction: Extract a set of objects(nodes) visible in the scene and considers possible relationships between all nodes by utilizing standard object detection pipeline.
2) Relationship edge pruning: Then relation proposal network prunes unlikely relationships using a learned measure of ‘relatedness’, producing a sparser candidate graph structure.
3) Graph context integration: Finally, an attentional graph convolution network is applied to integrate global context and update object node and relationship edge labels.

Relation Proposal Network learns to efficiently compute relatedness score between object pairs which are used to intelligently prune unlikely scene graph connections.

Given the resulting sparsely connected scene graph candidate, attentional Graph Convolution Network propagate higher-order context throughout the graph – updating each object(node) and relationship(edge) representation based on its neighbors. It predicts per-node edge attentions, enabling to learn to modulate information flow across unreliable edges.


# 정리

# Reference
[1] Johnson, J., Krishna, R., Stark, M., Li, L.J., Shamma, D.A., Bernstein, M., Fei-Fei, L.: "Image retrieval using scene graphs."" In: CVPR (2015)
[2] Xu, D., Zhu, Y., Choy, C.B., Fei-Fei, L.: "Scene graph generation by iterative message passing."" In: CVPR (2017)
[3] S. Ren, K. He, R. Girshick, and J. Sun.: "Faster R-CNN: Towards real-time object detection with region proposal networks." In: NIPS (2015)